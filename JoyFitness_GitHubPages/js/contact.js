document.addEventListener('DOMContentLoaded',()=>{const f=document.getElementById('contactForm');f.onsubmit=e=>{e.preventDefault();if(!f.reportValidity())return;const box=document.getElementById('contactNotice');box.hidden=false;box.innerHTML='<strong>Demo submitted.</strong> This GitHub Pages version has no server email backend, so no message was actually sent.';f.reset();box.scrollIntoView({behavior:'smooth',block:'center'});};});