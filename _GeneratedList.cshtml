@model List<JoyRiseFitness.Models.Workout>
@if (!Model.Any())
{
    <p>没有匹配的动作，换条件试试~</p>
}
else
{
    <div class="gen-list">
        @foreach (var w in Model)
        {
            <div class="gen-card" data-id="@w.Id">
                <img src="@w.ImgUrl" alt="@w.Name" />
                <h4>@w.Name</h4>
                <span>@w.Part · @w.Difficulty</span>
            </div>
        }
    </div>
}